# Rename to `.env.local` and populate with your real credentials.
GEMINI_API_KEY=your-gemini-api-key
# Optional: override the default model (models/gemini-1.5-flash-latest)
GEMINI_MODEL_ID=

# Orchestration strategy: simulated | gemini
ORCHESTRATION_STRATEGY=simulated

# MCP execution mode: dry-run (no external side effects) | live
MCP_EXECUTION_MODE=dry-run

# GitHub MCP configuration
MCP_GITHUB_TOKEN=
MCP_GITHUB_REPO_OWNER=
MCP_GITHUB_EXECUTABLE=npx
MCP_GITHUB_SERVER=@modelcontextprotocol/server-github
MCP_GITHUB_TOOLSETS=
